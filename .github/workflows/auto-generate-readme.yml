name: Reusable README Generator

on:
  workflow_call:
    inputs:
      working_dir:
        description: "Directory containing Terraform code"
        required: true
        type: string
      commit_message:
        description: "Commit message"
        required: false
        type: string
        default: "docs: auto-update README"

permissions:
  contents: write   # Required so workflow can commit

jobs:
  generate-docs:
    runs-on: ubuntu-latest

    steps:
      # 1.Checkout repo with full history and credentials
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true

      # 2.Fix Git permissions (sometimes needed on GitHub-hosted runners)
      - name: Fix git permissions
        run: sudo chown -R $USER:$USER .git

      # 3.Setup Terraform CLI
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      # 4.Run terraform-docs to inject docs into README
      - name: Run terraform-docs
        uses: terraform-docs/gh-actions@v1.0.0
        with:
          working-dir: ${{ inputs.working_dir }}
          config-file: ${{ inputs.working_dir }}/.terraform-docs.yml
          output-file: README.md
          output-method: inject

      # 5.Capture Terraform outputs (file_path, location, project)
      - name: Capture Terraform Outputs
        working-directory: ${{ inputs.working_dir }}
        run: |
          terraform output -json > tf_outputs.json
          echo "## Terraform Output Values" > outputs_section.md
          jq -r '. | to_entries[] | "- \(.key): \(.value.value)"' tf_outputs.json >> outputs_section.md

      # 6.Update README with actual Terraform outputs
      - name: Update Terraform outputs in README
        run: |
          sed -i '/<!-- BEGIN_REAL_OUTPUTS -->/,/<!-- END_REAL_OUTPUTS -->/d' ${{ inputs.working_dir }}/README.md
          echo '<!-- BEGIN_REAL_OUTPUTS -->' >> ${{ inputs.working_dir }}/README.md
          echo '## Actual Terraform Output Values' >> ${{ inputs.working_dir }}/README.md
          cat ${{ inputs.working_dir }}/outputs_section.md >> ${{ inputs.working_dir }}/README.md
          echo '<!-- END_REAL_OUTPUTS -->' >> ${{ inputs.working_dir }}/README.md

      # 7.Commit and push README.md changes
      - name: Commit README changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: ${{ inputs.commit_message }}
          file_pattern: ${{ inputs.working_dir }}/README.md
          commit_user_name: github-actions
          commit_user_email: github-actions@github.com
          push_options: --force
